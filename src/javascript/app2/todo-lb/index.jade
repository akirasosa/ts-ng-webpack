.container  #todoapp    header      h1.text-center ToDos backed by Spring Boot    section#content      .row        .col-md-12.top-buffer          form(role="form" name="todoCtrl.todoForm" ng-submit="todoCtrl.addTodo()")            input.form-control.input-lg(              type="text"              name="title"              ng-model="todoCtrl.newTodo.title"              placeholder="What needs to be done?"              required            )      .row        .col-md-12.top-buffer          ul.list-unstyled            li.top-buffer(ng-repeat="todo in todoCtrl.todos")              .input-group                span.input-group-addon                  input(type="checkbox" ng-model="todo.done")                input.form-control(type="text" ng-model="todo.title" ng-class="{strike: todo.done}")                span.input-group-btn                  button.btn.btn-default(type="button" ng-click="todoCtrl.removeTodo(todo)")                   i.glyphicon.glyphicon-remove      .row        .col-md-12.top-buffer         pagination(           total-items="todoCtrl.totalItems"           ng-model="todoCtrl.currentPage"           items-per-page="todoCtrl.itemsPerPage"           ng-change="todoCtrl.getTodos()"         )      .row        .col-md-12.top-buffer          button.btn.btn-warning(type="button" ng-click="todoCtrl.clearCompleted()")            | Clear completed